pipeline {
    agent any

    stages {

        stage('Checkout') {
            steps {
                echo 'Codigo obtenido desde GitHub'
            }
        }

        stage('Build') {
            steps {
                echo 'Compilando aplicacion'
            }
        }

        stage('Test') {
            steps {
                echo 'Ejecutando pruebas'
            }
        }
    }
}
